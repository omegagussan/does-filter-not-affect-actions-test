name: Actions

on:
  push:
    branches: [ test-GITHUB-ref ]
  pull_request:
    branches: [ main ]
    
jobs:
  my-action:
    name: This is one action I have
    runs-on: ubuntu-latest

    env:
      MY_ENV: 1234567

    steps:
      - name: Checkout source Git repo
        uses: actions/checkout@v2

      - name: run read-end
        run: ./read_env.sh
